# API Keys - Replace with your actual keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# NWS doesn't currently require an API key, but this is included for future-proofing
# or if you decide to use a different weather API that requires authentication
NWS_API_KEY=your_nws_api_key_here_if_needed

# Optional: Configure custom settings
# USER_AGENT=your-app-name/1.0 (your-email@example.com)